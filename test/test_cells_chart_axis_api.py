# coding: utf-8

"""
    Web API Swagger specification

    No description provided (generated by Swagger Codegen https://github.com/swagger-api/swagger-codegen)

    OpenAPI spec version: 1.0
    
    Generated by: https://github.com/swagger-api/swagger-codegen.git
"""


from __future__ import absolute_import

import os
import sys
import unittest
import warnings

ABSPATH = os.path.abspath(os.path.realpath(os.path.dirname(__file__)) + "/..")
sys.path.append(ABSPATH)
import asposecellscloud
from asposecellscloud.rest import ApiException
from asposecellscloud.apis.cells_api import CellsApi
from asposecellscloud.models.axis import Axis
import AuthUtil
global_api = None

class TestCellsChartAxisApi(unittest.TestCase):
    """ CellsChartAreaApi unit test stubs """

    def setUp(self):
        warnings.simplefilter('ignore', ResourceWarning)
        global global_api
        if global_api is None:
           global_api = asposecellscloud.apis.cells_api.CellsApi(AuthUtil.GetClientId(),AuthUtil.GetClientSecret(),"v3.0",AuthUtil.GetBaseUrl())
        self.api = global_api

    def tearDown(self):
        pass

    def test_cells_charts_get_chart_category_axis(self):
        """
        Test case for cells_charts_get_chart_category_axis

        Get/post chart axis info.
        """
        name ='Book1.xlsx'
        sheet_name ='Sheet4'
        chartIndex = 0  
        folder = "PythonTest"
        result = AuthUtil.Ready(self.api, name, folder)
        self.assertTrue(len(result.uploaded)>0) 
        result = self.api.cells_charts_get_chart_category_axis(name, sheet_name,chartIndex, folder=folder)
        self.assertEqual(result.code,200)
        pass

    def test_cells_charts_get_chart_second_category_axis(self):
        """
        Test case for cells_charts_get_chart_second_category_axis

        Get/post chart axis info.
        """
        name ='Book1.xlsx'
        sheet_name ='Sheet4'
        chartIndex = 0  
        folder = "PythonTest"
        result = AuthUtil.Ready(self.api, name, folder)
        self.assertTrue(len(result.uploaded)>0) 
        result = self.api.cells_charts_get_chart_second_category_axis(name, sheet_name,chartIndex, folder=folder)
        self.assertEqual(result.code,200)
        pass
    def test_cells_charts_get_chart_value_axis(self):
        """
        Test case for cells_charts_get_chart_value_axis

        Get/post chart axis info.
        """
        name ='Book1.xlsx'
        sheet_name ='Sheet4'
        chartIndex = 0  
        folder = "PythonTest"
        result = AuthUtil.Ready(self.api, name, folder)
        self.assertTrue(len(result.uploaded)>0) 
        result = self.api.cells_charts_get_chart_value_axis(name, sheet_name,chartIndex, folder=folder)
        self.assertEqual(result.code,200)
        pass
    def test_cells_charts_get_chart_second_value_axis(self):
        """
        Test case for cells_charts_get_chart_second_value_axis

        Get/post chart axis info.
        """
        name ='Book1.xlsx'
        sheet_name ='Sheet4'
        chartIndex = 0  
        folder = "PythonTest"
        result = AuthUtil.Ready(self.api, name, folder)
        self.assertTrue(len(result.uploaded)>0) 
        result = self.api.cells_charts_get_chart_second_value_axis(name, sheet_name,chartIndex, folder=folder)
        self.assertEqual(result.code,200)
        pass
    def test_cells_charts_get_chart_series_axis(self):
        """
        Test case for cells_charts_get_chart_series_axis

        Get/post chart axis info.
        """
        name ='Book1.xlsx'
        sheet_name ='Sheet4'
        chartIndex = 0  
        folder = "PythonTest"
        result = AuthUtil.Ready(self.api, name, folder)
        self.assertTrue(len(result.uploaded)>0) 
        result = self.api.cells_charts_get_chart_series_axis(name, sheet_name,chartIndex, folder=folder)
        self.assertEqual(result.code,200)
        pass
    

    def test_cells_charts_post_chart_category_axis(self):
        """
        Test case for cells_charts_post_chart_category_axis

        Get/post chart axis info.
        """
        name ='Book1.xlsx'
        sheet_name ='Sheet4'
        chartIndex = 0  
        folder = "PythonTest"
        axis = Axis()
        axis.min_value = 10.0
        result = AuthUtil.Ready(self.api, name, folder)
        self.assertTrue(len(result.uploaded)>0) 
        result = self.api.cells_charts_post_chart_category_axis(name, sheet_name,chartIndex, axis, folder=folder)
        self.assertEqual(result.code,200)
        pass

    def test_cells_charts_post_chart_second_category_axis(self):
        """
        Test case for cells_charts_post_chart_second_category_axis

        Get/post chart axis info.
        """
        name ='Book1.xlsx'
        sheet_name ='Sheet4'
        chartIndex = 0  
        folder = "PythonTest"
        axis = Axis()
        axis.min_value = 10.0
        result = AuthUtil.Ready(self.api, name, folder)
        self.assertTrue(len(result.uploaded)>0) 
        result = self.api.cells_charts_post_chart_second_category_axis(name, sheet_name,chartIndex, axis, folder=folder)
        self.assertEqual(result.code,200)
        pass
    def test_cells_charts_post_chart_value_axis(self):
        """
        Test case for cells_charts_post_chart_value_axis

        Get/post chart axis info.
        """
        name ='Book1.xlsx'
        sheet_name ='Sheet4'
        chartIndex = 0  
        folder = "PythonTest"
        axis = Axis()
        axis.min_value = 10.0
        result = AuthUtil.Ready(self.api, name, folder)
        self.assertTrue(len(result.uploaded)>0) 
        result = self.api.cells_charts_post_chart_value_axis(name, sheet_name,chartIndex,axis, folder=folder)
        self.assertEqual(result.code,200)
        pass
    def test_cells_charts_post_chart_second_value_axis(self):
        """
        Test case for cells_charts_post_chart_second_value_axis

        Get/post chart axis info.
        """
        name ='Book1.xlsx'
        sheet_name ='Sheet4'
        chartIndex = 0  
        folder = "PythonTest"
        axis = Axis()
        axis.min_value = 10.0
        result = AuthUtil.Ready(self.api, name, folder)
        self.assertTrue(len(result.uploaded)>0) 
        result = self.api.cells_charts_post_chart_second_value_axis(name, sheet_name,chartIndex, axis, folder=folder)
        self.assertEqual(result.code,200)
        pass
    def test_cells_charts_post_chart_series_axis(self):
        """
        Test case for cells_charts_post_chart_series_axis

        Get/post chart axis info.
        """
        name ='Book1.xlsx'
        sheet_name ='Sheet4'
        chartIndex = 0  
        folder = "PythonTest"
        axis = Axis()
        axis.min_value = 10.0
        result = AuthUtil.Ready(self.api, name, folder)
        self.assertTrue(len(result.uploaded)>0) 
        result = self.api.cells_charts_post_chart_series_axis(name, sheet_name,chartIndex,axis, folder=folder)
        self.assertEqual(result.code,200)
        pass


if __name__ == '__main__':
    unittest.main()
